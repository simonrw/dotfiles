// Zed keymap
//
// For information on binding keys, see the Zed
// documentation: https://zed.dev/docs/key-bindings
//
// To see the default key bindings run `zed: open default keymap`
// from the command palette.
[
  {
    "context": "Editor && vim_mode == normal",
    "bindings": {
      "f7": "debugger::StepInto",
      "f8": "debugger::StepOver",
      "f9": "debugger::Continue",
      "shift-f8": "debugger::StepOut",
      "t d b": "editor::ToggleBreakpoint"
    }
  },
  {
    "context": "vim_mode == visual",
    "bindings": {
      "space": null
    }
  },
  {
    "context": "vim_mode == visual && debugger_stopped",
    "bindings": {
      // "space e": "debugger::EvaluateSelectedText"
    }
  },
  {
    "context": "Workspace",
    "bindings": {
      // "shift shift": "file_finder::Toggle"
    }
  },
  {
    "context": "Editor && vim_mode == insert && !menu",
    "bindings": {
      // "j k": "vim::SwitchToNormalMode"
    }
  },
  {
    "context": "vim_mode == normal",
    "bindings": {
      // "space": null
    }
  },
  {
    "context": "AgentPanel || GitPanel || ProjectPanel || CollabPanel || OutlinePanel || ChatPanel || VimControl || EmptyPane || SharedScreen || MarkdownPreview || KeyContextView || DebugPanel",
    "bindings": {
      "ctrl-w c": "pane::CloseAllItems"
    }
  }
]
