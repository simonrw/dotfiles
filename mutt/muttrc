source ~/.mutt.d/solarized_light.muttrc
source "gpg2 -d ~/.mutt.d/passwords.gpg |"

# Global
set sort = reverse-date-received
set beep_new
set pipe_decode
set realname = "Simon Walker"
set fast_reply
set askcc
set fcc_attach
set forward_format = "Fwd: %s"
set forward_decode
set reply_to
set include
set forward_quote
set text_flowed=yes

set pager_index_lines = 10
set pager_context = 3
set pager_stop
set menu_scroll
set tilde
unset markers
# autoview html email
auto_view text/html
alternative_order text/plain text/enriched text/html

# IMAP
set copy=yes
set imap_idle
set header_cache="~/.mutt.d/headers"

# offlineimap
# set mbox_type=Maildir
# set folder=$HOME/Mail
# set spoolfile="+/Staff/INBOX"

set from = "s.walker.2@warwick.ac.uk"

# Keybindings
bind index <tab> sync-mailbox
bind index gg first-entry
bind index G last-entry
bind index <space> collapse-thread
bind index R group-reply
bind index o imap-fetch-mail

bind pager k previous-line
bind pager j next-line
bind pager gg top
bind pager G bottom
bind pager R group-reply
bind attach <return> view-mailcap

bind compose p postpone-message

# macro index o "<shell-escape>offlineimap<enter>"

# Contacts
set display_filter=~/.mutt.d/add-to-contacts.sh
set alias_file=~/.mutt.d/aliases.txt
set sort_alias=alias
set reverse_alias=yes
source $alias_file

# Account details
source ~/.mutt.d/authentication.mutt
