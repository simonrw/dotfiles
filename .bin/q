#!/usr/bin/env bash

set -euo pipefail

model="${Q_MODEL:-sonnet}"

while [[ $# -gt 0 ]]; do
    case "$1" in
        -m|--model) model="$2"; shift 2 ;;
        *) break ;;
    esac
done

claude --model "$model" "$*"
