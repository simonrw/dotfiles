# Handle nice suspend
xss-lock -- i3lock --color 333333 --show-failed-attempts &

# Set the screen configuration
xrandr --output DP-2 --primary --right-of DP-0

# Start the notification handler
/usr/bin/dunst &

# Configure keyboard map
bash ~/.bin/set-keyboard

# Start i3
exec i3
