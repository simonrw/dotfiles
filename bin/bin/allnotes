#!/bin/bash

set -eou pipefail

main() {
    (find ${HOME}/notes/src -name "*.md" | sort | xargs cat) >> /tmp/allnotes.md
    if command -v nvim 2>&1 >/dev/null; then
        nvim /tmp/allnotes.md
    else
        vim /tmp/allnotes.md
    fi
}

main
