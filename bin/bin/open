#!/bin/sh

set -eou pipefail

case $OSTYPE in
    darwin*)
        /usr/bin/open $*
        ;;
    linux*)
        /usr/bin/xdg-open $*
        ;;
    *)
        echo "No 'open' command found" >&2
        exit 1
esac
