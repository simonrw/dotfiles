#!/usr/bin/env bash

set -e


main() {
    if [[ "$(hostname -s)" == "ngtshead" ]]; then
        watch -n 1 qstat-long -u sw
    else
        ssh -t ngtshead.astro "watch -n 1 qstat-long -u sw"
    fi
}

main


