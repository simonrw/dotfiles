#!/usr/bin/env bash

set -e

main() {
    if [[ "$(hostname)" = *ngtshead* ]]; then
        watch qstat -u sw,rgw,philipp
    else
        ssh -t ngtshead.astro "watch qstat -u sw,rgw,philipp"
    fi
}

main
