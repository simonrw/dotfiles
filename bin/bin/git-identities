#!/bin/sh

# Print the list of available identities found in the gitconfig file

set -e

if command -v ggrep >/dev/null 2>&1; then
    # check if macos
    ggrep -oP '^\[user\s+"([^\\"]+)"\]' ~/.gitconfig | cut -d\" -f 2
else
    grep -oP '^\[user\s+"([^\\"]+)"\]' ~/.gitconfig | cut -d\" -f 2
fi
