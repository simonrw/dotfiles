#!/usr/bin/env bash

set -e

get_host_string() {
    seq -f 'ngts%02g' 1 1 11  | xargs | sed 's/ /,/g'
}

main() {
    HOSTS="$(get_host_string)"
    pdsh -w "${HOSTS},localhost" cat /proc/loadavg | sort
}

main
