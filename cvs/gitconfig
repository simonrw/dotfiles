[user]
    name = Simon Walker
    email = s.r.walker101@googlemail.com
[alias]
    st = status -s
    unstage = reset HEAD
    update = commit -am
    co = checkout
    oneline = log --pretty=oneline
    tree = log --graph --oneline --decorate --abbrev-commit
    atree = log --graph --oneline --all --decorate --abbrev-commit
    su = submodule update --init --recursive
    gpr = pull --rebase
    up = !git pull --rebase && git push
    cbranch = rev-parse --abbrev-ref HEAD
    upstream = !git rebase master && git checkout master && git merge --no-ff - && git checkout -
[color]
    ui = auto
[core]
    pager = less -FXRS
	excludesfile = ~/.cvsignore
    autocrlf = false
    safecrlf = true
    whitespace = fix
    editor = vim
    mergeoptions = --no-ff
[branch "master"]
    merge = master
    remote = origin
[github]
    user = mindriot101
    token = 7e549ed4dddc6ecc0bab22bcac87adac
[pager]
    diff =
[merge]
    tool = splice
	conflictstyle = diff3
[mergetool "splice"]
    cmd = "vim -f $BASE $LOCAL $REMOTE $MERGED -c 'SpliceInit'"
    trustExitCode = true
[push]
	default = current
