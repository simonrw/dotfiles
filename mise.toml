[tasks.all]
description = "Install/link packages"
depends = ["dotfiles", "defaults", "gh-extensions"]
depends_post = ["brew"]

[tasks.dotfiles]
description = "Install dotfiles"
run = "stow ."

[tools]
lua-language-server = "latest"
shellcheck = "latest"
stylua = "latest"

[tasks."brew:upgrade"]
run = "brew update && brew upgrade"
description = "Update brews"
