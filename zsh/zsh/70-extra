# Speed up git completion
__git_files() {
    _wanted files expl 'local files' _files
}

# Clever alias of the g command
function g {
    if [[ $# > 0 ]]; then
        git $@
    else
        git st
    fi
}
