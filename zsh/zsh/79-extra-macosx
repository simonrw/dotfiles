if [[ $OSTYPE =~ "^darwin.*$" ]]; then
    export PATH=${HOME}/.cabal/bin:${PATH}

		# Start rvm initialisation
		if [[ -s "$HOME/.rvm/scripts/rvm" ]] then
				. $HOME/.rvm/scripts/rvm
		fi

    # Some nice shell features
    source ${HOME}/.zsh/functions/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh
    source ${HOME}/.zsh/functions/zsh-history-substring-search/zsh-history-substring-search.zsh
fi
