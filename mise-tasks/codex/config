#!/usr/bin/env bash
#MISE description="Merge tracked Codex settings into system config"

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"

cd "$ROOT_DIR"
go run ./cmd/codex-config
